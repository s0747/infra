apiVersion: v1
kind: Service
metadata:
  name: myapp-service-clusterip
  namespace: k8s-wprawki
  labels:
    creator: created-by-XXX
    app: he-best-apka
    env: prod
spec:
  type: ClusterIP
  selector:
    app: myapp
    ver: 1-2
    tier: frontend
  ports:
    - port: 8080
      targetPort: 8080
