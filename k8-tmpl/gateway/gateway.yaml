apiVersion: gateway.networking.k8s.io/v1
kind: Gateway
metadata:
  name: glowna-brama
  namespace: default
spec:
  gatewayClassName: cilium # <--- Tutaj mówisz Kubernetesowi, że to Cilium ma obsłużyć ten ruch
  listeners:
    - name: http
      protocol: HTTP
      port: 80
      allowedRoutes:
        namespaces:
          from: Same
